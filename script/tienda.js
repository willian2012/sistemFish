// const addToShoppingCartButtons = document.querySelectorAll('.product-center');

// addToShoppingCartButtons.forEach(addToCartButton => {
//     addToCartButton.addEventListener('click', addToCArtClicked);
// });

// function addToCArtClicked(event){
//     const button = event.target;
//     const item = button.closest('.product-header');
    
    
//     const itemTitle = item.querySelector('.item-title').textContent;
//     const itemPrice = item.querySelector('.price').textContent;
//     const itemId = item.querySelector('.item-id').textContent;
//     const itemIva = item.querySelector('.item-iva').textContent;
//     const itemImpo = item.querySelector('.item-impo').textContent;
   
//     console.log(': addToCartClicked -> itemTitle', itemTitle, itemPrice, itemId, itemIva, itemImpo);


// }